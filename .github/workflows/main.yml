name: CF-Clean-IP-Scan
on:
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Scan IP
        run: |
          curl -sL https://raw.githubusercontent.com/vfarid/cf-clean-ip/main/run.sh | bash
